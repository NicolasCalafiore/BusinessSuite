<Window x:Class="BusinessSuite.SelfServe"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BusinessSuite"
        mc:Ignorable="d"
        Title="Menu" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="Button" x:Key="MenuButton">
            <Setter Property="Margin" Value="50"/>
            <Setter Property="Background" Value="LightBlue"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Style="{DynamicResource MenuButton}" Grid.Column="0"  Grid.Row="0" Content="Drinks" HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch" Margin="10" />

        <Button Style="{DynamicResource MenuButton}" Grid.Column="1"  Grid.Row="0" Content="Entrees" HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch" Margin="10" />

        <Button Style="{DynamicResource MenuButton}" Grid.Column="2"  Grid.Row="0" Content="Deserts" HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch" Margin="10" />

    </Grid>
</Window>
